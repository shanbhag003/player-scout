Screenshots of the running interface, referenced from the main README.

  01-landing.png    the landing page
  02-profile.png    a player profile with the radar and percentile table
  03-similar.png    the ranked results with filters
  04-compare.png    the comparison tab with several players
  05-metrics.png    the metric table from the comparison

Take them around 1400px wide, then Add file -> Upload files into this folder.
The charts in docs/figures are generated from the data and need no maintenance.
